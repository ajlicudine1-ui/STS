const form=document.getElementById("loginForm");const errorBox=document.getElementById("loginError");const loginBtn=document.getElementById("loginBtn");const password=document.getElementById("password");document.getElementById("togglePassword")?.addEventListener("click",e=>{const showing=password.type==="text";password.type=showing?"password":"text";e.currentTarget.textContent=showing?"Show":"Hide"});if(localStorage.getItem("devt_access_token")){window.location.replace("/")}form.addEventListener("submit",async e=>{e.preventDefault();errorBox.textContent="";loginBtn.disabled=true;loginBtn.textContent="Signing in...";try{const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:document.getElementById("email").value.trim(),password:password.value})});const result=await response.json();if(!response.ok)throw new Error(result.error||"Sign in failed.");localStorage.setItem("devt_access_token",result.access_token);localStorage.setItem("devt_profile",JSON.stringify(result.profile));window.location.replace("/")}catch(error){errorBox.textContent=error.message}finally{loginBtn.disabled=false;loginBtn.textContent="Sign In"}});