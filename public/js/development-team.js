const table=document.getElementById("membersTable"),modal=document.getElementById("memberModal"),form=document.getElementById("memberForm"),search=document.getElementById("memberSearch");let members=[];function esc(v){return String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;")}function fmt(d){if(!d)return"-";const x=new Date(d);return Number.isNaN(x.getTime())?String(d):x.toLocaleDateString()}function render(){const q=search.value.trim().toLowerCase(),rows=members.filter(m=>!q||`${m.full_name} ${m.email}`.toLowerCase().includes(q));document.getElementById("memberCount").textContent=members.length;document.getElementById("activeCount").textContent=members.filter(m=>m.is_active!==false).length;if(!rows.length){table.innerHTML='<tr><td colspan="5">No development team accounts found.</td></tr>';return}table.innerHTML=rows.map(m=>`<tr><td><strong>${esc(m.full_name)}</strong></td><td>${esc(m.email)}</td><td><span class="account-status ${m.is_active!==false?"active":"inactive"}">${m.is_active!==false?"Active":"Inactive"}</span></td><td>${esc(fmt(m.created_at))}</td><td><div class="account-actions"><button data-edit="${esc(m.user_id)}">Edit</button><button class="danger" data-delete="${esc(m.user_id)}">Delete</button></div></td></tr>`).join("");table.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>openEdit(b.dataset.edit));table.querySelectorAll("[data-delete]").forEach(b=>b.onclick=()=>deleteMember(b.dataset.delete))}async function load(){const r=await fetch("/api/development-team"),x=await r.json();if(r.status===403){window.location.replace("/");return}if(!r.ok)throw new Error(x.error||"Failed to load accounts");members=x.members||[];render()}function openNew(){form.reset();document.getElementById("editingUserId").value="";document.getElementById("memberModalTitle").textContent="Add Development Team Member";document.getElementById("passwordLabel").textContent="Temporary Password *";document.getElementById("passwordHint").textContent="The member can use this password to sign in immediately.";document.getElementById("activeField").hidden=true;document.getElementById("saveMemberBtn").textContent="Create Account";document.getElementById("memberFormError").textContent="";modal.classList.add("show")}function openEdit(id){const m=members.find(x=>x.user_id===id);if(!m)return;form.reset();document.getElementById("editingUserId").value=id;document.getElementById("memberFullName").value=m.full_name||"";document.getElementById("memberEmail").value=m.email||"";document.getElementById("memberActive").checked=m.is_active!==false;document.getElementById("memberModalTitle").textContent="Edit Development Team Member";document.getElementById("passwordLabel").textContent="New Password";document.getElementById("passwordHint").textContent="Leave blank to keep the current password.";document.getElementById("activeField").hidden=false;document.getElementById("saveMemberBtn").textContent="Save Changes";document.getElementById("memberFormError").textContent="";modal.classList.add("show")}function close(){modal.classList.remove("show");form.reset()}async function deleteMember(id){const m=members.find(x=>x.user_id===id);if(!m||!confirm(`Delete the account for ${m.full_name}?\n\nExisting project history will remain, but the account will no longer be able to sign in.`))return;const r=await fetch(`/api/development-team/${encodeURIComponent(id)}`,{method:"DELETE"}),x=await r.json();if(!r.ok){alert(x.error||"Unable to delete account");return}await load()}form.addEventListener("submit",async e=>{e.preventDefault();const id=document.getElementById("editingUserId").value,payload={full_name:document.getElementById("memberFullName").value.trim(),email:document.getElementById("memberEmail").value.trim(),password:document.getElementById("memberPassword").value};if(id)payload.is_active=document.getElementById("memberActive").checked;const btn=document.getElementById("saveMemberBtn"),err=document.getElementById("memberFormError");err.textContent="";btn.disabled=true;try{const r=await fetch(id?`/api/development-team/${encodeURIComponent(id)}`:"/api/development-team",{method:id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)}),x=await r.json();if(!r.ok)throw new Error(x.error||"Unable to save account");close();await load()}catch(error){err.textContent=error.message}finally{btn.disabled=false}});document.getElementById("addMemberBtn").onclick=openNew;document.getElementById("closeMemberModal").onclick=close;document.getElementById("cancelMemberBtn").onclick=close;search.oninput=render;modal.addEventListener("click",e=>{if(e.target===modal)close()});document.addEventListener("DOMContentLoaded",()=>setTimeout(()=>load().catch(e=>{table.innerHTML=`<tr><td colspan="5">${esc(e.message)}</td></tr>`}),0));